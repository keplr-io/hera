import React, {Component, PropTypes} from 'react';
import './style.scss';

export default class Dashboard extends Component {

    static propTypes = {
    }

    componentWillMount() {
    }

    render() {
        return <div>dash</div>;
    }
};
